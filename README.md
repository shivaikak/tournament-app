# tournament-app
A Java-based command-line application for managing sports tournaments. Users can create tournaments, register teams, generate round-robin or single-elimination schedules, view standings, matches and champions. The project applies object-oriented design principles and strategy-based scheduling to keep the system modular and easy to extend.
